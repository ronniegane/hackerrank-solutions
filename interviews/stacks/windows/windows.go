package windows

/*
https://www.hackerrank.com/challenges/min-max-riddle/problem

Given an input array of length n,
for each window length 1...n calculate the minimum value of each window
return an array that describes the maximum value of these minimums
for each window length

seems similar to the builings problem
*/

func riddle(arr []int64) []int64 {

	ret := make([]int64, len(arr))

	return ret
}
